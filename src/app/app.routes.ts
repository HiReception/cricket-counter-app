import { Routes } from '@angular/router';
import { ScorerPaneComponent } from './scorer-pane/scorer-pane.component';

export const routes: Routes = [
    {path: "", component: ScorerPaneComponent}
];
